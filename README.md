# Transfer-map demo (front-end)

Draws a network graph of which addresses sent transfers to which.  
Backend: https://github.com/vknowable/transfer-map-backend/tree/master

## Usage

1. Set the `REACT_APP_API_URL` to the api endpoint of your backend in the .env file
2. `npm install` and `npm start` to start the dev server
